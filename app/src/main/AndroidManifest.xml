<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidtest">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.AndroidTest"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.screens.logreg.RegisterActivity"></activity>
        <activity android:name=".activity.screens.logreg.LoginActivity" />
        <activity android:name=".activity.screens.dresschooser.DressChooser" />
        <activity android:name=".activity.screens.dressdetails.DressDetails" />
        <activity android:name=".activity.screens.introduction.IntroductionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>